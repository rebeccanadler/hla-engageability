# code/

Analysis code, exported from the Claude Science / Claude Code sessions. Suggested modules:

- `engageability/` — crest footprint + per-allele metric
- `decomposition/` — HED, capacity, axis orthogonality
- `identifiability/` — symbolic verification (sympy)
- `somatic_selection/` — dNdScv driver (R)
- `loh/` — Bradley-Terry, exact-binomial, negative controls

Each figure in `../docs/FIGURE_INDEX.md` should map to a script here.
