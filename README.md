# arms/

One folder per experimental arm (see ../MANIFEST.md for the map). Each arm folder should contain its `results.md`, `methods.md`, figures, provenance ledger, and small derived tables. Populate with `../assemble_local.sh` or by hand.
