# Python dependencies for the HLA engageability project.
# Pin exact versions before submitting: `pip freeze > environment/requirements.lock.txt`
# The versions below are typical/known-good ranges — replace with your session's exact pins.

numpy>=1.24
pandas>=2.0
scipy>=1.10
statsmodels>=0.14
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
biopython>=1.81
requests>=2.31
tqdm>=4.65
sympy>=1.12          # identifiability arm (symbolic verification)
jupyter>=1.0         # notebooks/

# Notes:
# - dN/dS uses dNdScv, which is R (see environment/R_environment.md), not a Python package.
# - NetMHCpan-4.1, EvoEF2, LOHHLA, MSIsensor/MANTIS are external binaries (see DATA_ACCESS.md).
