name: hla-engageability
channels:
  - conda-forge
  - bioconda
dependencies:
  - python>=3.10
  - numpy>=1.24
  - pandas>=2.0
  - scipy>=1.10
  - statsmodels>=0.14
  - scikit-learn>=1.3
  - matplotlib>=3.7
  - seaborn>=0.12
  - biopython>=1.81
  - requests>=2.31
  - tqdm>=4.65
  - sympy>=1.12
  - jupyter>=1.0
  # R + dNdScv are managed separately — see environment/R_environment.md
  # NetMHCpan-4.1, EvoEF2, LOHHLA, MSIsensor/MANTIS are external binaries — see DATA_ACCESS.md
